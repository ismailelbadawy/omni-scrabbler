#include <map>
#include <string.h>

using namespace std;

class SuperLeaveLoader
{
private:
    /* data */
public:
    SuperLeaveLoader() { }
    SuperLeaveLoader(map<string, double> *, map<char,  double> *, string, string);
    ~SuperLeaveLoader() { }
};
